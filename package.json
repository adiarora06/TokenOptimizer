{
  "name": "token-optimizer",
  "version": "0.9.0",
  "description": "Browser-based AI workspace that turns one rough request into a completed result through adaptive routing, compact handoff contracts, and inspectable usage.",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.cjs",
    "test": "npm run test:core && npm run test:api && npm run test:extension",
    "test:core": "node tests/optimizer-core.test.cjs",
    "test:api": "node tests/api-endpoints.test.cjs",
    "test:extension": "node extensions/gemini-token-optimizer/tests/sidepanel-logic.test.cjs && node extensions/gemini-token-optimizer/tests/content-gemini.test.cjs && node extensions/gemini-token-optimizer/tests/content-chatgpt.test.cjs"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "//devDependencies": "Browser libraries only. Their shipped files are vendored into outputs/vendor/ and served statically, so the serverless runtime must not install them.",
  "devDependencies": {
    "@tabler/icons-webfont": "^3.45.0",
    "dompurify": "^3.4.12",
    "marked": "^18.0.6"
  }
}
